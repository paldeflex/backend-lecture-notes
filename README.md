# Модуль 1. Вкатун (intern)

## Интернет. ОС. Linux
### HTTP
- [Как работает HTTP протокол?](#как-работает-http-протокол)
- [Чем отличается HTTP 1.1 от HTTP 2.0?](#чем-отличается-http-11-от-http-20)
- [REST API](#rest-api)
- [Из чего состоит HTTP запрос?](#из-чего-состоит-http-запрос)
- [Какие существуют HTTP методы?](#какие-существуют-http-методы)
- [Какие есть коды ответов в HTTP?](#какие-есть-коды-ответов-в-http)
- [Чем отличается 401 и 403?](#чем-отличается-401-и-403)
- [За что отвечает каждая группа: 1-5xxx](#за-что-отвечает-каждая-группа-1-5xxx)
- [Что такое идемпотентность?](#что-такое-идемпотентность)
- [Какие методы идемпотентны?](#какие-методы-идемпотентны)
- [PUT vs PATCH](#put-vs-patch)
- [Назови основные заголовки](#назови-основные-заголовки)
### ОС
- [Назови отличие между многопоточностью и многопроцессностью](#назови-отличие-между-многопоточностью-и-многопроцессностью)
- [Многопоточность vs ассинхронность](#многопоточность-vs-ассинхронность)
- [Race Condition](#race-condition)
### Linux
- [Как подключиться к удаленной машине? (ssh)](#как-подключиться-к-удаленной-машине-ssh)
- [Как вывести определенное количество строк из файла с конца?](#как-вывести-определенное-количество-строк-из-файла-с-конца)
- [Как смотреть состояние сервисов?](#как-смотреть-состояние-сервисов)
- [sed, awk, grep](#sed-awk-grep)
- [Расскажи про систему прав](#расскажи-про-систему-прав)
### Интернет
- [Что происходит после ввода адреса в адресную строку и нажатия на Enter?](#что-происходит-после-ввода-адреса-в-адресную-строку-и-нажатия-на-enter)
## PHP. Основы
- [Назови типы данных в PHP](#назови-типы-данных-в-php)
- [Какой тип данных передается по ссылке?](#какой-тип-данных-передается-по-ссылке)
- [Отличие == и ===](#отличие--и-)
- [PHP интерпретируемый или компилируемый? Почему?](#php-интерпретируемый-или-компилируемый-почему)
- [Отличие ассоциативного и обычного массива](#отличие-ассоциативного-и-обычного-массива)
## ООП. Основы. Composer
- [Назови 3 принципа ООП](#назови-3-принципа-ооп)
- [За что отвечает абстракция?](#за-что-отвечает-абстракция)
- [Абстрактный класс vs интерфейс](#абстрактный-класс-vs-интерфейс)
- [Как в памяти хранится экземпляр объекта?](#как-в-памяти-хранится-экземпляр-объекта)
- [Перечисли магические методы](#перечисли-магические-методы)
- [Модификаторы доступа и примеры](#модификаторы-доступа-и-примеры)
- [Методы полиморфизма](#методы-полиморфизма)
- [Наследование интерфейсов](#наследование-интерфейсов)
- [Что можно указывать в интерфейсе?](#что-можно-указывать-в-интерфейсе)
- [Ключевые слова this, self, static, parent](#ключевые-слова-this-self-static-parent)
- [Когда использовать статику?](#когда-использовать-статику)
- [Реализация инкапсуляции](#реализация-инкапсуляции)
- [Множественное наследование и его эмуляция](#множественное-наследование-и-его-эмуляция)
- [require vs include, require_once vs require](#require-vs-include-require_once-vs-require)
- [composer.json vs composer.lock](#composerjson-vs-composerlock)
- [require vs require_dev](#require-vs-require_dev)
- [Для чего нужен dump-autoload?](#для-чего-нужен-dump-autoload)
- [install vs update](#install-vs-update)
- [Какой из файлов будешь отправлять в удаленный репозиторий composer.lock или composer.json?](#какой-из-файлов-будешь-отправлять-в-удаленный-репозиторий-composerlock-или-composerjson)
## GIT. Основы
- [Git merge vs rebase](#git-merge-vs-rebase)
- [Опиши git flow](#опиши-git-flow)
- [fetch vs pull](#fetch-vs-pull)
- [cherry-pick — в каких случаях использовать](#cherry-pick--в-каких-случаях-использовать)
- [quash commits](#squash-commits)
- [Как отменить последний коммит](#как-отменить-последний-коммит)
- [В каких случаях возникают конфликты и как их решать](#в-каких-случаях-возникают-конфликты-и-как-их-решать)
## SQL. Основы
- [Что такое JOIN и какие бывают?](#что-такое-join-и-какие-бывают)
- [Как работает B-TREE индекс?](#как-работает-b-tree-индекс)
- [B-TREE balanced vs binary](#b-tree-balanced-vs-binary)
- [Плюсы и минусы индексов](#плюсы-и-минусы-индексов)
- [having vs where](#having-vs-where)
- [Что делает GROUP BY](#что-делает-group-by)
- [Нормализация (3НФ), денормализация](#нормализация-3нф-денормализация)
- [char vs varchar](#char-vs-varchar)
- [Ключи в таблице](#ключи-в-таблице)
- [Селективность индекса](#селективность-индекса)
- [Составной индекс](#составной-индекс)
- [pk vs fk](#pk-vs-fk)
- [sql vs nosql](#sql-vs-nosql)
- [jsonb и его применение](#jsonb-и-его-применение)
- [Виды ограничений](#виды-ограничений)

## Как работает HTTP-протокол
1. Браузер анализирует URL и понимает, что по этому адресу нужно отправить HTTP-запрос
    > Этот пункт актуален, если клиент переходит по ссылке браузера. Но это не обязательно: иногда HTTP-запрос формируется после каких-то действий пользователя автоматически.
2. Клиент формирует и отправляет запрос. Для этого генерируется стартовая строка, заголовки и тело запроса в зависимости от того, что именно будет запрошено у сервера.
3. Запрос направляется напрямую на сервер либо через прокси. Движение запроса регулируется другими протоколами, которые управляют отправкой данных по сети. Обычно это TCP/IP.
4. Сервер получает запрос, обрабатывает и формирует ответ. Он также заполняет стартовую строку и заголовок, а в тело поместит то, что клиент запросил.
5. Клиент получает ответ и отображает результат. Обычно для этого он расшифровывает полученные данные в HTML и отображает их в браузере в виде веб-страницы.

![Такой путь проходит HTTP-запрос, когда пользователь хочет открыть главную страницу Практикума или любого другого сайта
](./img/1.webp)

[Четыре буквы, на которых держится интернет: что такое протокол HTTP и почему на нём работают почти все сайты](https://practicum.yandex.ru/blog/chto-takoe-protokol-http/)

## Чем отличается HTTP 1.1 от HTTP 2.0?
| Особенность | HTTP/1.1 | HTTP/2 |
|--------------|-----------|--------|
| **Формат** | Текстовый | Бинарный |
| **Передача данных** | Последовательная (1 запрос за раз) | Параллельная (мультиплексирование) |
| **Сжатие заголовков** | gzip/DEFLATE | HPACK — быстрее и безопаснее |
| **Server Push** | Нет | Сервер может сам отправлять ресурсы заранее |
| **Производительность** | Медленнее при множестве запросов | Быстрее (на 30–50%) |
| **Безопасность** | TLS не обязателен | Почти всегда через TLS |

---

[Ультимативный гайд по HTTP. HTTP/1.1 vs HTTP/2](https://habr.com/ru/companies/avito/articles/710678/)


## REST API
| Раздел | Кратко | Что важно запомнить |
|--------|---------|--------------------|
| **Что такое REST API** | Архитектурный стиль взаимодействия клиента и сервера через HTTP. | Работает по HTTP, ресурсы имеют уникальные URI, используются стандартные методы (GET, POST, PUT, DELETE). |
| **Основная идея** | Клиент → запрос, сервер → ответ (JSON). | Каждый запрос независим, сервер не хранит состояние. |
| **Принципы REST** | 1️⃣ Клиент–сервер; 2️⃣ Stateless; 3️⃣ Кеширование; 4️⃣ Единообразие интерфейса; 5️⃣ Слои; 6️⃣ Код по требованию. | Stateless и единообразие интерфейса спрашивают почти всегда. |
| **Ключевые компоненты** | Ресурсы (URI), HTTP-методы, заголовки, коды ответов. | Всё строится вокруг ресурсов и методов CRUD. |
| **Методы HTTP** | GET — чтение, POST — создание, PUT — полное обновление, PATCH — частичное обновление, DELETE — удаление. | GET и PUT — идемпотентны, POST — нет. |
| **Формат данных** | JSON (иногда XML). | JSON — стандарт де-факто. |
| **Коды ответов** | 200 OK, 201 Created, 400 Bad Request, 401 Unauthorized, 404 Not Found, 500 Internal Server Error. | Обязательно знать 200/201/400/401/404/500. |
| **Безопасность** | HTTPS, аутентификация (Basic, OAuth 2.0, JWT), rate-limiting, валидация данных. | Использовать HTTPS и токены. |
| **Тестирование** | Postman, Swagger (OpenAPI), pytest/JUnit. | Проверяют корректность методов, коды и формат JSON. |
| **Где применяется** | Веб-сервисы, мобильные приложения, IoT, DevOps. | Почти любое приложение сегодня использует REST API. |

---

### 🔍 Принципы REST подробно

1. **Клиент–сервер (Client–Server)**  
   Клиент отвечает за интерфейс и отправку запросов, сервер — за хранение и обработку данных.  
   Такое разделение делает систему независимой: можно обновлять фронтенд, не трогая сервер.

2. **Отсутствие состояния (Stateless)**  
   Каждый запрос — самостоятельный.  
   Сервер не хранит информацию о предыдущих запросах клиента.  
   Всё, что нужно для выполнения — должно быть в самом запросе (заголовки, токены и т.д.).

3. **Кеширование (Caching)**  
   Ответы сервера можно сохранять (Cache-Control, ETag), чтобы не запрашивать данные повторно.  
   Это снижает нагрузку и ускоряет работу.

4. **Единообразие интерфейса (Uniform Interface)**  
   Общие правила взаимодействия: единый формат URL, стандартные HTTP-методы, одинаковая структура запросов/ответов.  
   Благодаря этому API становится предсказуемым и удобным для интеграции.

5. **Система уровней (Layered System)**  
   Между клиентом и сервером могут быть промежуточные слои: прокси, балансировщики, системы логирования.  
   Каждый слой выполняет свою функцию, не зная деталей других.

6. **Код по требованию (Code on Demand)** *(необязательный)*  
   Сервер может отправлять клиенту исполняемый код (например, JS-скрипт), который тот выполняет.  
   Используется редко, но заложено в концепцию REST.

---

[REST API: что это такое и как работает](https://practicum.yandex.ru/blog/chto-takoe-rest-api-i-kak-rabotaet/)

## Из чего состоит HTTP запрос?

HTTP-запрос — это сообщение, которое клиент (например, браузер) отправляет серверу, чтобы получить ресурс или выполнить действие.  
Он состоит из **трёх основных частей**:

---

### ⚙️ Структура HTTP-запроса

1. **Стартовая строка (Request Line)**  
   Содержит:
    - **Метод** — действие, которое нужно выполнить (`GET`, `POST`, `PUT`, `DELETE` и др.);
    - **Путь (URI)** — адрес ресурса на сервере (`/index.html`, `/api/users`);
    - **Версию протокола** (`HTTP/1.1`, `HTTP/2`).

   **Пример:**
   ```
   GET /index.html HTTP/1.1
   ```

2. **Заголовки (Headers)**  
   Передают дополнительную информацию:
    - тип содержимого (`Content-Type`),
    - данные об агенте (`User-Agent`),
    - предпочтительный язык (`Accept-Language`),
    - авторизацию (`Authorization`)  
      и другие параметры.

   **Пример:**
   ```
   Host: example.com
   User-Agent: Mozilla/5.0
   Accept: text/html
   ```

3. **Тело запроса (Body)** *(необязательно)*  
   Содержит данные, которые клиент отправляет серверу (обычно в `POST` или `PUT`).  
   Например: данные формы, JSON, XML и т.п.

   **Пример:**
   ```json
   {
     "name": "Alice",
     "age": 25
   }
   ```

---

### 💡 Полный пример HTTP-запроса

```http
POST /api/users HTTP/1.1
Host: example.com
Content-Type: application/json
Authorization: Bearer 123abc

{
  "name": "Alice",
  "age": 25
}
```

[HTTP-запросы: структура, методы, строка статуса и коды состояния](https://habr.com/ru/articles/865040)

## Какие существуют HTTP методы?
HTTP-метод — это "команда" в первой строке запроса, которая говорит серверу, **что именно сделать с ресурсом**: прочитать, создать, изменить или удалить.  
У каждого метода есть свойства: безопасность (не меняет состояние), идемпотентность (повтор даёт тот же результат) и возможность кеширования.

---

### ⚙️ Основные HTTP-методы

| Метод   | Безопасный | Идемпотентный | Кешируемый\* | Назначение |
|---------|-----------|---------------|--------------|------------|
| **GET** | да        | да            | да           | Получение данных (страницы, API-ответы) без изменения состояния сервера. |
| **HEAD** | да       | да            | да           | То же, что GET, но возвращает только заголовки без тела. Удобен для проверок и кеша. |
| **POST** | нет      | нет           | обычно нет   | Создание ресурсов или операции с побочными эффектами (формы, логин, запуск процессов). |
| **PUT** | нет       | да            | нет          | Полная замена ресурса по конкретному URI (клиент присылает целый новый объект). |
| **PATCH** | нет    | зависит\**    | нет          | Частичное обновление ресурса (изменяем только нужные поля). |
| **DELETE** | нет    | да            | нет          | Удаление ресурса. В проде часто реализуется как "мягкое" удаление. |
| **OPTIONS** | да     | да            | нет          | Узнать, какие методы и заголовки поддерживает ресурс (часто в CORS). |


\* `Кешируемый (cacheable)` — это такой HTTP-метод, результаты которого можно сохранять (кешировать) и повторно использовать при следующих запросах, чтобы не обращаться к серверу заново.

\** `PATCH` по стандарту может быть неидемпотентным, но реализация может сделать его идемпотентным (например, при использовании JSON Merge Patch).

---

### 💡 Пример использования методов

```http
GET /articles/100 HTTP/1.1
Host: example.com
Accept: application/json
```

```http
POST /articles HTTP/1.1
Host: example.com
Content-Type: application/json

{
  "title": "Новая статья",
  "body": "Текст статьи..."
}
```

```http
PATCH /articles/100 HTTP/1.1
Host: example.com
Content-Type: application/merge-patch+json

{
  "title": "Обновлённый заголовок"
}
```

[HTTP-методы без мистики: что нужно сайту по умолчанию](https://www.securitylab.ru/analytics/563109.php)

## Какие есть коды ответов в HTTP?

HTTP-коды состояний — это стандартизированные трёхзначные числа, которые сервер возвращает в ответ на HTTP-запрос.  
Они показывают, **чем закончилась обработка запроса**: успех, редирект, ошибка клиента или сервера.

Статус всегда приходит в строке вида:

```http
HTTP/1.1 200 OK
```

где `200` — код, а `OK` — текстовое описание.

---

### Классификация кодов: 1xx–5xx

Класс кода определяется **первой цифрой**:

| Диапазон | Категория            | Описание                                                         |
|----------|----------------------|------------------------------------------------------------------|
| **1xx**  | Информационные       | Запрос принят, обработка продолжается                           |
| **2xx**  | Успешные             | Запрос успешно выполнен                                         |
| **3xx**  | Перенаправления      | Нужны дополнительные действия (обычно — новый запрос по другому URI) |
| **4xx**  | Ошибки клиента       | Проблема в запросе или правах доступа                           |
| **5xx**  | Ошибки сервера       | Сервер не смог корректно обработать запрос                      |

---

### Примеры по группам

#### 1xx — информационные

Используются редко, в основном в специфичных сценариях (длинные запросы, смена протокола):

- **100 Continue** — сервер принял заголовки, клиент может отправлять тело запроса.
- **101 Switching Protocols** — сервер согласен переключиться на другой протокол (например, WebSocket).
- **102 Processing** — запрос принят, но обработка ещё продолжается.
- **103 Early Hints** — сервер заранее отправляет заголовки для предзагрузки ресурсов.

#### 2xx — успешные

Запрос принят, понят и обработан:

- **200 OK** — стандартный успешный ответ (страница, данные API и т.п.).
- **201 Created** — ресурс успешно создан (часто после `POST /api/...`).
- **202 Accepted** — запрос принят, но обработка ещё не завершена (асинхронные операции).
- **204 No Content** — запрос обработан, но тела ответа нет (часто при `DELETE` или `PUT`).
- **206 Partial Content** — возвращена только часть ресурса (дозагрузка/докачка файлов).

#### 3xx — перенаправления

Сервер говорит клиенту: «Нужно обратиться по другому адресу или с другим условием».

- **301 Moved Permanently** — ресурс навсегда переехал на новый URI.
- **302 Found** — временное перенаправление.
- **304 Not Modified** — ресурс не изменился; клиент может использовать кеш.
- **307 Temporary Redirect** — временный редирект с сохранением метода (GET остаётся GET).
- **308 Permanent Redirect** — постоянный редирект с сохранением метода.

#### 4xx — ошибки клиента

Проблема в запросе, данных или правах пользователя:

- **400 Bad Request** — неверный запрос (кривой JSON, неправильные параметры и т.п.).
- **401 Unauthorized** — требуется аутентификация (нужно «представиться» системе).
- **403 Forbidden** — аутентификация есть, но прав доступа к ресурсу нет.
- **404 Not Found** — ресурс не найден (не существует или скрыт).
- **405 Method Not Allowed** — ресурс не поддерживает этот HTTP-метод (например, `POST` на эндпоинт только с `GET`).
- **429 Too Many Requests** — слишком много запросов (rate limiting, защита от спама/DDoS).

#### 5xx — ошибки сервера

Сервер получил корректный запрос, но **сам не справился** с обработкой:

- **500 Internal Server Error** — общее указание на внутреннюю ошибку сервера.
- **502 Bad Gateway** — прокси/шлюз получил некорректный ответ от следующего сервера.
- **503 Service Unavailable** — сервер временно недоступен (перегрузка, техработы).
- **504 Gateway Timeout** — истек таймаут при обращении к upstream-серверу.

---

### Ключевые коды, которые нужно знать в первую очередь

В повседневной разработке чаще всего встречаются:

- **200** — успешный ответ.
- **201** — создан новый ресурс.
- **204** — успешная операция без тела ответа.
- **301 / 302 / 307 / 308** — различные варианты редиректов.
- **304** — работает кеш (ресурс не изменился).
- **400** — ошибка в запросе.
- **401 / 403** — проблемы с аутентификацией/авторизацией.
- **404** — ресурс не найден.
- **429** — слишком много запросов.
- **500 / 502 / 503 / 504** — разные виды проблем на стороне сервера/инфраструктуры.

---

[HTTP коды состояний: полное руководство для веб-разработчиков](https://sky.pro/wiki/analytics/http-kody-sostoyanij-polnoe-rukovodstvo-dlya-veb-razrabotchikov)


## Чем отличается 401 и 403?
**401 (Unauthorized)** указывает, что клиент не аутентифицирован и требует корректных данных для доступа. Возникает, если пользователь вводит неверный логин и пароль или в том случае, когда их нет в базе данных системы. Ещё одна причина ошибки 401 — срабатывание защиты сайта от ботов.

**403 (Forbidden)** означает, что клиент аутентифицирован, но не обладает достаточными правами для доступа к ресурсу. Сервер понимает запрос клиента, но отказывается его выполнить, так как клиент не обладает необходимыми разрешениями. В отличие от 401 Unauthorized, личность клиента известна серверу.

## За что отвечает каждая группа: 1-5xxx
1xx — информационные сообщения, означают, что сервер принял запрос и продолжает его обработку. 
Примеры:
* 100 Continue — клиент может продолжать отправку тела запроса
* 101 Switching Protocols — сервер переключается на другой протокол (например, WebSocket).

2xx — сообщения об успехе, означают, что запрос успешно обработан.
Примеры:
* 200 OK — стандартный успешный ответ
* 201 Created — ресурс успешно создан
* 204 No Content — успешный запрос, но тело ответа отсутствует.

3xx — коды перенаправления, означают, что для завершения запроса клиент должен выполнить дополнительное действие (например, перейти по другому URL).
Примеры:
* 301 Moved Permanently — ресурс перемещён навсегда
* 302 Found — временное перенаправление
* 304 Not Modified — ресурс не изменился, можно использовать кеш.

4xx — коды ошибки клиента, означают, что запрос некорректен или клиент не имеет прав.
Примеры:
* 400 Bad Request — синтаксическая ошибка в запросе
* 401 Unauthorized — требуется аутентификация
* 403 Forbidden — доступ запрещён
* 404 Not Found — ресурс не найден.

5xx — коды ошибки сервера, означают, что сервер не смог выполнить корректный запрос из-за внутренней ошибки. 
Примеры:
* 500 Internal Server Error — общая внутренняя ошибка
* 502 Bad Gateway — шлюз получил неверный ответ от сервера
* 503 Service Unavailable — сервис временно недоступен (например, на обслуживании)
* 504 Gateway Timeout — таймаут при обращении к внешнему серверу.

## Что такое идемпотентность?

Идемпотентность — это свойство операции, при котором **повторное выполнение одного и того же действия не меняет состояние системы** по сравнению с тем, как если бы оно было выполнено один раз.

Грубо говоря:  
сделал 1 раз → система изменилась;  
сделал 10 раз то же самое → система в том же состоянии, что и после первого раза.

---

**Примеры из жизни**

- **Кнопка лифта**  
  Вы нажали кнопку вызова лифта → лифт поехал к вам.  
  Жмёте кнопку ещё 10 раз — лифт не поедет «ещё раз», он просто уже едет или уже приехал.  
  Повторные действия **не меняют результат** → поведение идемпотентно.

- **Покупки в магазине**  
  Кладёте лимон в корзину → стало 1 лимон.  
  Кладёте ещё один → стало 2.  
  Каждое действие **меняет состояние** (количество лимонов растёт) → это уже **неидемпотентно**.

---

**Идемпотентность в HTTP-методах**

В контексте HTTP идемпотентность важна для надёжности и повторных запросов:

- **GET** — читает данные, не меняет состояние системы. Сколько ни вызывай один и тот же `GET /users/1` — пользователь не изменится.
- **PUT** — полная замена ресурса. Если отправлять **одни и те же данные** в `PUT /users/1`, после первого запроса последующие ничего не меняют — результат тот же → метод считается идемпотентным.
- **DELETE** — удаляет ресурс. Первый раз ресурс удаляется, повторные `DELETE` уже не меняют систему (ресурса всё равно нет) → идемпотентен по контракту.
- **POST** — создаёт или изменяет данные **каждый раз**: повторный запрос может создать дубликат или выполнить действие ещё раз → **неидемпотентен**.

Идемпотентность здесь важна для безопасных повторов: если запрос потерялся или клиент не получил ответ, можно смело отправить его ещё раз (для идемпотентных методов) без риска «сломать» данные.

---

**Искусственная идемпотентность (идемпотентные ключи)**

Даже для неидемпотентных операций (например, платежей через `POST`) можно **симулировать идемпотентность**:

- клиент добавляет к запросу `idempotency_key` (уникальный идентификатор операции);
- сервер запоминает, что запрос с этим ключом уже был выполнен, и при повторе:
    - не создаёт новый платёж;
    - возвращает результат первой операции.

Так избегают **двойных списаний** и дублирующих операций при сетевых сбоях или повторных кликах пользователя.

---

[Идемпотентность: искусство не менять мир дважды](https://habr.com/ru/articles/868382/)

## Какие методы идемпотентны?
Идемпотентными являются HTTP-методы, повторное выполнение которых дает тот же результат, что и однократное, без непредвиденных побочных эффектов
. К идемпотентным методам относятся GET, HEAD, PUT, DELETE, OPTIONS и TRACE. В отличие от них, методы POST и PATCH обычно неидемпотентны, поскольку их повторные вызовы могут приводить к разным результатам.

**Идемпотентные методы**

* *GET:* Извлекает данные из ресурса. Повторные запросы не изменяют состояние сервера и возвращают одинаковые данные (если ресурс не был изменен другим запросом).
* *HEAD:* Аналогичен GET, но возвращает только заголовки, а не тело ответа.
* *PUT:* Полностью заменяет ресурс на сервере новым содержимым. Последующие запросы PUT с теми же данными не изменят состояние системы по сравнению с первым запросом.
* *DELETE:* Удаляет ресурс. Повторный запрос на удаление уже удаленного ресурса не изменит состояние сервера.
* *OPTIONS:* Описывает параметры взаимодействия с ресурсом (например, какие HTTP-методы поддерживаются).

**Неидемпотентные методы**

* *POST:* Используется для создания нового ресурса. Повторные запросы, как правило, создают несколько одинаковых ресурсов, что приводит к разным результатам.
* *PATCH:* Используется для частичного обновления ресурса. Его идемпотентность зависит от конкретной реализации. Если операция частичного обновления не является идемпотентной, повторные вызовы могут давать разные результаты. 

## PUT vs PATCH

Оба метода предназначены для **изменения ресурса** на сервере,  
но делают это по-разному — **PUT заменяет ресурс целиком**, а **PATCH изменяет только часть**.

---

### ⚙️ Ключевое различие

| Характеристика | **PUT** | **PATCH** |
|----------------|----------|-----------|
| **Назначение** | Полная замена ресурса | Частичное изменение ресурса |
| **Тип данных в теле** | Новая версия ресурса целиком | Набор изменений (инструкций) |
| **Идемпотентный** | ✅ Да — повторный запрос не изменяет результат | ⚠️ Нет (может изменять состояние при повторе) |
| **Кэшируемый** | ❌ Нет | ❌ Нет |
| **Ответ сервера** | Может не содержать тело (`204 No Content`) | Часто содержит обновлённые данные (`200 OK`) |
| **Используется в HTML-формах** | ❌ Нет | ❌ Нет |

---

### 💡 Объяснение простыми словами

- **PUT**  
  Клиент отправляет **весь обновлённый объект**.  
  Сервер **перезаписывает** старую версию полностью.

  **Пример:**
  ```http
  PUT /users/5 HTTP/1.1
  Content-Type: application/json

  {
    "name": "Alice",
    "email": "alice@example.com"
  }
  ```
  После этого объект пользователя на сервере содержит **только** эти поля.  
  Если какие-то поля были раньше (например, `age`), они **удаляются** — ресурс заменён целиком.

---

- **PATCH**  
  Клиент отправляет **только изменённые поля** или **инструкции по обновлению**.  
  Сервер применяет их **поверх существующего ресурса**.

  **Пример:**
  ```http
  PATCH /users/5 HTTP/1.1
  Content-Type: application/json

  {
    "email": "new@example.com"
  }
  ```
  В этом случае изменится только поле `email`, остальные данные пользователя останутся прежними.

---

### 🧠 Идемпотентность

- **PUT** — идемпотентен: повтор одного и того же запроса не меняет результат.
- **PATCH** — не всегда идемпотентен: если запрос добавляет элемент или изменяет счётчик, повторный вызов может снова изменить ресурс.

---

### 📎 Кратко

> **PUT** = полная замена ресурса.  
> **PATCH** = частичное обновление ресурса.
>
> Используй **PUT**, если у тебя есть вся новая версия объекта.  
> Используй **PATCH**, если нужно изменить только конкретные поля.

---

[Разница (отличия) между PUT и PATCH в REST](https://ru.stackoverflow.com/questions/1070324/)

## Назови основные заголовки
| Заголовок | Где используется | Назначение | Пример |
|------------|------------------|-------------|---------|
| **Host** | запрос | Указывает, к какому домену обращается клиент (обязателен в HTTP/1.1). | `Host: example.com` |
| **Content-Type** | запрос / ответ | Определяет тип данных, передаваемых в теле запроса или ответа. | `Content-Type: application/json` |
| **Authorization** | запрос | Передаёт токен доступа или креды пользователя для аутентификации. | `Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9` |
| **Accept** | запрос | Указывает, какие форматы ответов клиент готов принять. | `Accept: application/json` |
| **User-Agent** | запрос | Сообщает серверу информацию о клиенте (браузере, ОС, API-клиенте). | `User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64)` |


## Назови отличие между многопоточностью и многопроцессностью
**Многопоточность (multithreading)** и **многопроцессность (multiprocessing)** — это два подхода к параллельному выполнению задач,  
но они различаются тем, **как используется память и ресурсы процессора**.

---

### ⚙️ Ключевое различие

| Критерий | **Многопоточность** | **Многопроцессность** |
|-----------|---------------------|------------------------|
| **Что создаётся** | Потоки (threads) внутри одного процесса | Отдельные независимые процессы |
| **Область памяти** | Общая память (все потоки видят одни и те же данные) | У каждого процесса своя память |
| **Обмен данными** | Быстрый, но требует синхронизации (`mutex`, `lock`) | Медленный, через межпроцессное взаимодействие (IPC, сокеты, очереди) |
| **Изоляция** | Слабая — ошибка одного потока может «уронить» всё приложение | Сильная — сбой одного процесса не влияет на другие |
| **Ресурсоёмкость** | Легче и быстрее (меньше памяти) | Тяжелее (отдельная память и контекст для каждого процесса) |
| **Пример использования** | Сервер, обрабатывающий множество клиентов в одном приложении | Распределённые вычисления, независимые службы, воркеры |

---

### 💡 Пример в жизни

- **Многопоточность** — как официанты в одном ресторане:  
  работают параллельно, но кухня (память) у них общая.
- **Многопроцессность** — как отдельные рестораны:  
  каждый со своей кухней, меню и персоналом.

---

### 🧠 Кратко

> Многопоточность — несколько потоков в **одном** процессе, общая память, но риск конфликтов.  
> Многопроцессность — несколько **независимых** процессов, больше изоляции, но выше затраты на ресурсы.

---

## Многопоточность vs ассинхронность
🧵 1. Многопоточность

Ты даёшь процессору много потоков — допустим, 3.

Каждый поток делает свою задачу,
и операционная система раздаёт процессорное время каждому по очереди,
чтобы казалось, что они работают одновременно.

```http request
Поток 1: █████░░░░░░░
Поток 2: ░░█████░░░░░
Поток 3: ░░░░░░█████░
```
⏳ Процессор на самом деле прыгает между ними очень быстро
(миллионы переключений в секунду).
Если у тебя несколько ядер — действительно выполняются параллельно.

⚡ 2. Асинхронность

А теперь у тебя один поток, одно ядро, один исполнитель.
Он тоже обрабатывает кучу задач,
но работает только тогда, когда есть смысл.

Например:

отправил запрос в интернет — теперь ждёт ответ;

пока ответ не пришёл, зачем стоять без дела?

он переключается на другую задачу, которая уже готова выполняться.
```http request
Задача 1: ███░░░░░░░░ (ждёт ответ от сети)
Задача 2: ░░░███░░░░░ (пишет в файл)
Задача 3: ░░░░░░███░░ (парсит JSON)
```

🔧 Пример для мозга

🧵 Многопоточность — 5 работников, каждый занят своим делом.
Они реально работают параллельно.

⚡ Асинхронность — 1 работник, но с отличной памятью и планом.
Он говорит:
«Пока кофе варится — я сбегаю отнести письмо, а потом вернусь за кофе».
Никто не стоит без дела.

## Race Condition
**Race Condition** - Это ситуация, когда два или более потока (или процесса) пытаются одновременно получить доступ к общему ресурсу,
и результат выполнения программы зависит от порядка их выполнения. Это может привести к непредсказуемым результатам или ошибкам в работе программы.

Чтобы избежать требуется:
- Использование блокировок (Locks):
  Механизмы блокировок позволяют потокам или процессам получать эксклюзивный доступ к общим ресурсам.
  Когда один поток захватывает блокировку, другие потоки должны ждать, пока она не будет освобождена. Это гарантирует, что только один поток может изменять общий ресурс в определенный момент времени.

- Использование пессимистический контроль в SQL:
  C помощью оператора «SELECT ... FOR UPDATE. Чтобы снять блокировку, пользователь может подтвердить или отменить транзакцию: COMMIT; ROLLBACK

- Оптимистичный контроль параллелизма
  Добавить столбец для хранения «номера версии», который увеличивается при каждом обновлении.
  UPDATE Room SET available = FALSE, version = version + 1 WHERE id = 123 AND version = 1;

- Использование паттерна Unit of Work
- Использование идемпотентности запросов


## Как подключиться к удаленной машине? (ssh)
Подключение к удалённой машине выполняется через SSH-клиент
```angular2html
ssh -p 2222 user@host
```
Для авторизации обычно используют SSH-ключи (`~/.ssh/id_rsa`), что безопаснее пароля.
После успешного подключения получаем удалённый терминал сервера.

## Как вывести определенное количество строк из файла с конца?
Команда tail:
```angular2html
tail -n 10 filename.txt # выведет последние 10 строк файла.
```

## Как смотреть состояние сервисов?
```angular2html
systemctl status service_name
systemctl list-units --type=service # Посмотреть все активные сервисы
systemctl is-active service_name # Для краткой проверки, запущен ли серви
```

## sed, awk, grep
grep — искать строки
Используется, когда нужно найти строки, где встречается шаблон/слово.
```angular2html
grep "error" app.log          # все строки с "error" в файле
grep -i "error" app.log       # то же, но без учёта регистра
grep -r "TODO" .              # рекурсивно по всем файлам в каталоге
grep -n "error" app.log       # показать номер строки
```

sed — потоковый редактор (редактирует текст «на лету»)
Нужен, когда надо заменить текст, удалить/вывести определённые строки без открытия файла в редакторе.
```angular2html
sed 's/foo/bar/g' file.txt        # заменить "foo" на "bar" во всех местах строки
sed -n '10,20p' file.txt          # вывести строки с 10 по 20
sed '/error/d' file.txt           # удалить строки, содержащие "error" (на выводе)
```

```angular2html
sed -i 's/foo/bar/g' file.txt

делает следующее:

sed — потоковый редактор текста.
-i — редактировать файл на месте (in-place).
s/foo/bar/g — во всех строках выполнить замену:
s — substitute (замена),
foo — что ищем,
bar — на что меняем,
g — глобально в строке (все вхождения, а не только первое).

file.txt — файл, в котором это всё делается.
```

awk — мини-язык для работы с «табличным» текстом
Удобен, когда нужно работать с колонками (разделитель — пробел/запятая/таб и т.п.), что-то считать/фильтровать.

Есть файл data.txt:
```angular2html
id name score
1 Alex 90
2 Kate 85
3 Max 100
```

```angular2html
awk '{print $1, $3}' data.txt
```

Вывод
```angular2html
id score
1 90
2 85
3 100
```


## Расскажи про систему прав 
В Linux у каждого файла и каталога есть владелец, группа и права доступа.
Права делятся на три типа:

* r — чтение (read)
* w — запись (write)
* x — выполнение (execute)

И задаются для трёх категорий пользователей:

* user (владелец)
* group (пользователи группы)
* others (все остальные)

```angular2html
drwxr-xr--
```
* d — это директория
* rwx — владелец: читать, писать, заходить
* r-x — группа: читать и заходить
* r-- — остальные: только читать

## Что происходит после ввода адреса в адресную строку и нажатия на Enter?

1. Браузер разбирает адрес: протокол (`https`), домен (`example.com`), путь (`/home`), параметры.

2. Браузер или ОС ищет IP-адрес домена:
    - проверяет кеш браузера;
    - потом системный кеш или файл `hosts`;
    - если не найдено — обращается к DNS-серверу провайдера или публичному.
      В итоге получает IP, например `93.184.216.34`.

3. Устанавливается соединение:
    - для HTTP — обычное TCP (порт 80);
    - для HTTPS — сначала TCP (порт 443), потом TLS-рукопожатие: обмен сертификатами, шифрование, проверка подлинности сайта.

4. Браузер формирует и отправляет HTTP-запрос:

   ```
   GET /home HTTP/1.1
   Host: example.com
   User-Agent: Chrome/142
   ```

5. Веб-сервер принимает запрос (например, Nginx → PHP-FPM).
   Далее выполняется backend-логика:
    - роутинг, контроллеры;
    - запросы в базу данных;
    - генерация HTML или JSON-ответа.

6. Сервер возвращает ответ клиенту:

   ```
   HTTP/1.1 200 OK
   Content-Type: text/html
   <body>...</body>
   ```

7. Браузер получает ответ, строит DOM-дерево,
   подгружает CSS, JS, изображения,
   и формирует визуальную страницу.

8. Дополнительно:
    - могут использоваться кеш, cookies, service workers;
    - при HTTPS возможно возобновление TLS-сессии без нового рукопожатия.

---

**Кратко:**  
DNS → TCP/TLS → HTTP-запрос → сервер → ответ → отрисовка страницы.

## Назови типы данных в PHP
* Скалярные типы — это простые, неделимые значения.,- **boolean** — логический тип
  ```php
  $isReady = true;
  $hasAccess = false;
  ```

- **integer** — целое число
  ```php
  $age = 25;
  $temperature = -5;
  ```

- **float (double)** — число с плавающей точкой
  ```php
  $price = 19.99;
  $pi = 3.1415;
  ```

- **string** — строка символов
  ```php
  $name = "Pavlik";
  $greeting = 'Hello, world!';
  ```

* Составные типы (Compound types) - могут содержать несколько значений или более сложные структуры
- **array** — набор элементов (индексных или ассоциативных)
  ```php
  $nums = [1, 2, 3];
  $user = ['name' => 'Pavlik', 'age' => 25];
  ```

- **object** — экземпляр класса
  ```php
  class User {
      public string $name;
      public function __construct($name) {
          $this->name = $name;
      }
  }
  $u = new User('Pavlik');
  ```

- **callable** — функция, которую можно вызвать
  ```php
  function hello() { echo "Hi!"; }
  $callback = 'hello';
  $callback(); // Hi!
  ```

- **iterable** — всё, по чему можно пройти в foreach (массивы, объекты Traversable)
  ```php
  function printItems(iterable $data) {
      foreach ($data as $item) echo $item;
  }
  printItems([1, 2, 3]);
  ```
* Специальные типы

- **resource** — ссылка на внешний ресурс (файл, соединение и т.д.)
  ```php
  $handle = fopen("file.txt", "r");
  var_dump($handle); // resource
  ```

- **null** — отсутствие значения
  ```php
  $value = null;
  ```


## Какой тип данных передается по ссылке?
* Объекты всегда передаются по ссылке — при присвоении или передаче в функцию копируется не сам объект, а ссылка на него.
* Остальные типы (скалярные, массивы) по умолчанию передаются по значению, если явно не указать &.

## Отличие == и ===
* == — нестрогое сравнение: сначала приводит операнды к одному типу, потом сравнивает значения.
Пример: "5" == 5 → true, "0" == false → true.

* === — строгое сравнение: сравнивает и значение, и тип, без приведения типов.
Пример: "5" === 5 → false, 5 === 5 → true.


## PHP интерпретируемый или компилируемый? Почему?
PHP — интерпретируемый язык, потому что его код выполняется построчно интерпретатором (PHP Engine), без предварительной компиляции в машинный код.

Однако при выполнении PHP-файлы сначала компилируются во внутренний байт-код (opcodes), который потом исполняется Zend Engine — это ускоряет работу, но не делает язык компилируемым в классическом смысле.

## Отличие ассоциативного и обычного массива
* Обычный массив — индексы идут по порядку, числовые (0, 1, 2...).
    > Пример: [10, 20, 30].

* Ассоциативный массив — индексы — это строки или произвольные ключи.
    > Пример: ['name' => 'Павлик', 'age' => 25].

## Назови 3 принципа ООП
1. Инкапсуляция — объединяет данные (свойства) и методы в один объект (хранит и данные, и действия с ними вместе). Скрывает детали реализации от внешнего кода.
   Пример: свойства private, доступ через getters и setters.
    ```php
    class User {
        private string $name;
    
        public function setName(string $name) {
            $this->name = $name;
        }
    
        public function getName(): string {
            return $this->name;
        }
    }
    
    $user = new User();
    $user->setName('Павлик');
    echo $user->getName(); // Павлик
    ```

2. Наследование — позволяет создавать новый класс на основе существующего.
   Дочерний класс получает свойства и методы родителя и может их расширять или переопределять.
    ```php
    class Animal {
        public function makeSound() {
            echo "Some sound";
        }
    }
    
    class Dog extends Animal {
        public function makeSound() {
            echo "Bark";
        }
    }
    
    $dog = new Dog();
    $dog->makeSound(); // Bark
    ```
3. Полиморфизм — разные классы могут иметь методы с одинаковыми именами, но разным поведением.
    ```php
    class Animal {
        public function makeSound() {
            echo "Some sound";
        }
    }
    
    class Dog extends Animal {
        public function makeSound() {
            echo "Bark";
        }
    }
    
    $dog = new Dog();
    $dog->makeSound(); // Bark
    ```

## За что отвечает абстракция?
*Абстракция* — это способ скрыть лишние детали и работать только с важным поведением.
В ООП — это интерфейсы и абстрактные классы,
а в целом — любой способ упростить сложную систему, чтобы с ней было легче взаимодействовать.

```php
// Этот интерфейс — абстракция. Он описывает что нужно сделать (pay()), но не говорит, как именно.

class CardPayment implements Payment {
    public function pay(float $sum) { echo "Оплата $sum картой"; }
}

class CryptoPayment implements Payment {
    public function pay(float $sum) { echo "Оплата $sum криптой"; }
}
```

## Абстрактный класс vs интерфейс
🧩 Абстрактный класс

Это класс, который нельзя создать напрямую,
он служит для описания общей логики и структуры для потомков.
Может содержать как абстрактные методы (без реализации), так и обычные методы и свойства.

🧩 Интерфейс

Это контракт, который говорит:

“Любой класс, реализующий меня, обязан иметь такие-то методы.”

Интерфейс не содержит реализацию, только описание того, что должно быть реализовано.

## Как в памяти хранится экземпляр объекта?
* Ссылку на класс (то есть на его описание — какие свойства и методы есть).
* Таблицу свойств объекта — имена и значения его конкретных переменных.
* Счётчик ссылок — сколько переменных указывают на этот объект.

```php
$a = new stdClass();
$b = $a;

// Переменные $a и $b не копируют объект,
// а просто указывают на одну и ту же область памяти (один экземпляр).
// Если изменить $b->x = 5, то $a->x тоже изменится.
```

## Перечисли магические методы
* `__construct()` — вызывается при создании объекта (конструктор).
* `__destruct()` — вызывается при уничтожении объекта.
* `__get($name)` — обращение к несуществующему или приватному свойству.
* `__set($name, $value)` — присвоение значения недоступному свойству.
* `__call($name, $args)` — вызов несуществующего метода.
* `__callStatic($name, $args)` — вызов несуществующего статического метода.
* `__toString()` — возвращает строковое представление объекта при echo.
* `__invoke()` — позволяет вызывать объект как функцию.

## Модификаторы доступа и примеры
Модификаторы определяют, **где можно обращаться к свойствам и методам** класса.

Есть три основных:

| Модификатор | Доступен где | Пример |
|--------------|--------------|--------|
| **public** | везде — и внутри класса, и снаружи | метод/свойство видно всем |
| **protected** | внутри класса и у наследников | недоступен извне |
| **private** | только внутри самого класса | даже наследники не видят |

---

### 📘 Пример

```php
class User {
    public string $name = 'Павлик';
    protected string $role = 'user';
    private string $password = '12345';

    public function show() {
        echo $this->name;      // можно
        echo $this->role;      // можно
        echo $this->password;  // можно
    }
}

class Admin extends User {
    public function showRole() {
        echo $this->role;      // можно (protected видно наследникам)
        // echo $this->password; // ❌ нельзя (private видно только в User)
    }
}

$u = new User();
echo $u->name;     // ✅ public видно
// echo $u->role;   // ❌ protected недоступен
// echo $u->password; // ❌ private недоступен

$a = new Admin();
$a->showRole();    // ✅ работает
```

## Методы полиморфизма


## Наследование интерфейсов


## Что можно указывать в интерфейсе?


## Ключевые слова this, self, static, parent


## Когда использовать статику?


## Реализация инкапсуляции


## Множественное наследование и его эмуляция


## require vs include, require_once vs require


## composer.json vs composer.lock


## require vs require_dev


## Для чего нужен dump-autoload?


## install vs update


## Какой из файлов будешь отправлять в удаленный репозиторий composer.lock или composer.json?


## Git merge vs rebase


## Опиши git flow


## fetch vs pull


## cherry-pick — в каких случаях использовать


## squash commits


## Как отменить последний коммит


## В каких случаях возникают конфликты и как их решать


## Что такое JOIN и какие бывают?

## Что такое JOIN и какие бывают?


## Как работает B-TREE индекс?


## B-TREE balanced vs binary


## Плюсы и минусы индексов


## having vs where


## Что делает GROUP BY


## Нормализация (3НФ), денормализация


## char vs varchar


## Ключи в таблице


## Селективность индекса


## Составной индекс


## pk vs fk


## sql vs nosql


## jsonb и его применение


## Виды ограничений